# Dipendenze di SVILUPPO / TEST / quarantena locale.
# NON vanno in produzione ne' nell'immagine Docker.
# Installa prima tutto il runtime di produzione, poi i tool locali.
-r requirements.txt

# Vector store locale (alternativa gratuita ad Azure AI Search per dev/test).
chromadb>=0.5
# LLM/embedding locali gratuiti (alternativa ad Azure OpenAI per dev/test).
ollama>=0.3
# Runner dei test.
pytest>=8.0
# Client Hugging Face usato dai tool locali (es. modelli di embedding offline).
huggingface_hub>=0.20
